# Binary Builders

Single-page presence site at [binary.builders](https://binary.builders). Built with Vite.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Output is in `dist/`. Pushes to `main` deploy to GitHub Pages via `.github/workflows/deploy.yml`.
